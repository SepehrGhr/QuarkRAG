apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-query-to-llm
  namespace: quarkrag
spec:
  podSelector:
    matchLabels:
      app: llm-provider-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: query-service
      ports:
        - protocol: TCP
          port: 8000
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-query-to-llm
  namespace: quarkrag
spec:
  podSelector:
    matchLabels:
      app: query-service
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: llm-provider-service
      ports:
        - protocol: TCP
          port: 8000
