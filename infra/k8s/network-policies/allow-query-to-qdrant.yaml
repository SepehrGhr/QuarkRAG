apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-query-to-qdrant
  namespace: quarkrag
spec:
  podSelector:
    matchLabels:
      app: qdrant
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: query-service
      ports:
        - protocol: TCP
          port: 6333
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-query-to-qdrant
  namespace: quarkrag
spec:
  podSelector:
    matchLabels:
      app: query-service
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              app: qdrant
      ports:
        - protocol: TCP
          port: 6333
